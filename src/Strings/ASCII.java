package Strings;

public class ASCII {
        public static void main(String[] args) {
            char ch = 'H';

            int ascii = (int) ch;

            System.out.println("ASCII value of '" + ch + "' is: " + ascii);
        }
    }

